# Open-Omniscience - Core Requirements
# Minimum dependencies required to run the core Open-Omniscience platform
# 
# For full installation options, see:
# - requirements-core.txt  (this file, minimal dependencies)
# - requirements-llm.txt   (core + LLM support)
# - requirements-all.txt   (all dependencies including all pillars)
#
# Install with: pip install -r requirements.txt

# Include core requirements
-r requirements-core.txt
